/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["help.twitter.com", "img.freepik.com", "images.cnbctv18.com"],
	},
};

module.exports = nextConfig;
